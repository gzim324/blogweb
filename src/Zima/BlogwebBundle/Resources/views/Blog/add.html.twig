{% extends '@ZimaBlogweb/base.html.twig' %}

{% block body %}
    <h1>Add Contents</h1>
    {{ form_start(form, {"attr": {"novalidate": "novalidate"}}) }}
    <div class="form-group">
        {{ form_widget(form.title, {'attr': {'placeholder': 'Title'}}) }}
    </div>
    <div class="form-group">
    {{ form_widget(form.tags, {'attr': {'placeholder': 'tags'}}) }}
    </div>
    <div class="form-group">
    {{ form_widget(form.contents, {'attr': {'placeholder': 'Contents…'}}) }}
    </div>
    {{ form_end(form) }}



{% endblock %}